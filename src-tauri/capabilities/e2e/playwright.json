{
  "$schema": "../../gen/schemas/desktop-schema.json",
  "identifier": "e2e-testing",
  "description": "Only loaded in an `--features e2e-testing` build (see build.rs) — lets the frontend invoke `pw_result`, which tauri-plugin-playwright's control server needs to return eval results back to Rust.",
  "windows": ["*"],
  "permissions": [
    "playwright:default"
  ]
}
